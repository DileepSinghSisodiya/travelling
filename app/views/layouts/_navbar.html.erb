<nav class="navbar navbar-expand-lg bg-body-secondary" data-bs-theme="dark">
    <%= link_to root_path, class:"logo-link" do %>
      <%= image_tag 'logo.png', style: 'height:40px;width:auto;' %>
    <% end %>
      <%#= image_tag asset_url('logo.png') %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="#">Features</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Pricing</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">About</a>
          </li>
        </ul>
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-light" type="submit">Search</button>
        </form>
        	<div>
      			<% if user_signed_in? %>
        			Logged in as <strong><%= current_user.email %></strong>.
        			<%= button_to "Logout", destroy_user_session_path, method: :delete, :class => "btn btn-outline-light"%>
      			<% else %>
        			<%= link_to "Sign up", new_user_registration_path, :class => "btn btn-outline-light"%>
        			<%= link_to "Sign in", new_user_session_path, :class => "btn btn-outline-light"%>
      			<% end %>
			</div>
        <!-- <a href="<%"user_signed_in"%>" class="btn btn-outline-light" type="submit">Login</a>
        <a href="#" class="btn btn-outline-light" type="submit">Log out</a>
        <a href="#" class="btn btn-outline-light" type="submit">Sign up</a> -->
      </div>
    </div>
  </nav>